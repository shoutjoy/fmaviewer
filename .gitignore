# Browser-side AI model binaries are downloaded/generated locally.
/image_model/

# Publish only Code.gs so admin.html can display and copy the current Apps Script source.
# Local deployment notes and any future files in this folder remain excluded.
/Auth/gas/*
!/Auth/gas/Code.gs
